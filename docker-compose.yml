version: '3.8'

services:
  web:
    build: .
    image: john-park-site
    container_name: john-park-site
    restart: unless-stopped
    ports:
      - "3030:3030"
    environment:
      - NODE_ENV=production
      - PORT=3030
      # Add other env vars here if needed
